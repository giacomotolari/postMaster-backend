// In this file we will configure the environment variables and other configurations for the application.
// Benefits: we can use the configurations in different files and we can change the configurations in one place.
import dotenv from "dotenv";
dotenv.config();
